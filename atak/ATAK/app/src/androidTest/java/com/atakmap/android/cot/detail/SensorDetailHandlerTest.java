
package com.atakmap.android.cot.detail;

import androidx.test.ext.junit.runners.AndroidJUnit4;

import com.atakmap.android.androidtest.ATAKInstrumentedTest;

import org.junit.Test;
import org.junit.runner.RunWith;

@RunWith(AndroidJUnit4.class)
public class SensorDetailHandlerTest extends ATAKInstrumentedTest {

    @Test
    public void toItemMetadata() {
        /**
        Marker m = new Marker("test");
        CotEvent cotEvent = new CotEvent();
        CotDetail cd = new CotDetail("sensor");
        cd.setAttribute(SensorDetailHandler.FOV_ATTRIBUTE, "20.0");
        cd.setAttribute(SensorDetailHandler.AZIMUTH_ATTRIBUTE, "10.0");
        cd.setAttribute(SensorDetailHandler.RANGE_ATTRIBUTE, "5.0");
        
        SensorDetailHandler handler = new SensorDetailHandler();
        handler.toItemMetadata(m, cotEvent, cd);
        Assert.assertEquals(20.0, m.getMetaDouble(SensorDetailHandler.FOV_ATTRIBUTE, Double.NaN), 0.0001);
        Assert.assertEquals(10.0, m.getMetaDouble(SensorDetailHandler.AZIMUTH_ATTRIBUTE, Double.NaN), 0.0001);
        Assert.assertEquals(5.0, m.getMetaDouble(SensorDetailHandler.RANGE_ATTRIBUTE, Double.NaN), 0.0001);
         **/
    }
}
